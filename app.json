{
    "name": "Christmas Cheer",
    "description": "Twilio + littleMits + Christmas",
    "repository": "https://github.com/rickyrobinett/littleBits-Twilio-mp3Player",
    "keywords": ["twilio", "PHP", "laravel", "MMS", "littleBits"],
    "env": {
      "CLOUDBIT_ID": {
        "description": "Your littleBits cloudBit device id",
        "required": true
      },
      "CLOUDBIT_TOKEN": {
        "description": "Your littleBits cloudBit auth token",
        "required": true
      }
    }
}
